SUMMARY


SOURCE
https://kc7cyber.com/challenges/108

STEPS TAKEN
Section 0: KQL 101
How many employees are in the company?
Employees
| count
Answer: 888

Which employee has the IP address: “10.10.0.20”?
Employees
| where ip_addr == "10.10.0.20"
Answer: Rose Boyd
Notes: email - rose_boyd@deathcon-scholomance.io, username - roboyd, role - Enchantment Marketing Specialist, hostname - MMH9-MACHINE

How many emails did Elsie Hall receive?
let el_email=
Employees
| where name == "Elsie Hall"
| distinct email_addr;
Email
| where recipient in (el_email)
Answer: 47

How many distinct senders were seen in the email logs that contain links with scholomance-suppliers.online?
Email
| where link contains "scholomance-suppliers.online"
| distinct sender
Answer: 403

How many unique URLs did “Becky Thrower” visit?
let becky_ip=
Employees
| where name == "Becky Thrower"
| distinct ip_addr;
OutboundNetworkEvents
| where src_ip in (becky_ip)
| distinct url
Answer: 80

How many unique domains in the PassiveDns records contain the word “documents”?
PassiveDns
| where domain contains "documents"
| distinct domain
Answer: 4
Notes: details-documents.org, detailsdocuments.org, information-documents.org, informationdocuments.org

What IPs did the domain “information-documents.org” resolve to (enter any one of them)?
PassiveDns
| where domain == "information-documents.org"
| distinct ip
Answer: 1
Notes: 187.4.28.70

How many unique URLs were browsed by employees named “Karen”?
let karen_ips=
Employees
| where name contains "Karen"
| distinct ip_addr;
OutboundNetworkEvents
| where src_ip in (karen_ips)
| distinct url
Answer: 204


Section 1: They Call Me CRIMSON
There was a lot of emails going around about regulation and compliance. Search for these terms in the Emails table. When is the first time a sender with either of these terms show up?(Note: Let's ignore emails from our trusted partner companies for now)
Email
| where sender contains "regulation" or sender contains "compliance"
| where verdict != "CLEAN"
Answer: 10/10/2023, 9:01:05 AM

How many sender email addresses are linked together by the same theme?
Email
| where sender contains "regulation" or sender contains "compliance" and sender !contains "researchcompliance.biz"
| distinct sender
Answer: 4
Notes: executivecompliance@verizon.com, executive_compliance@protonmail.com, executive_compliance@yahoo.com, compliance.compliance@verizon.com

How many unique subject lines were sent by these email addresses?
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| distinct subject
Answer: 6

Which of these subject lines was used to target the fewest number of users?
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| summarize count() by subject
Answer: [EXTERNAL] IMPORTANT: Provide your information now.

Let's take all of the email addresses and look for how many distinct URLs were sent by them. How many unique URLs were sent?
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| distinct link
Answer: 8

How many employees clicked on these links?
let urls=
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| distinct link;
OutboundNetworkEvents
| where url in (urls)
| distinct src_ip
Answer: 20

How many distinct employee roles clicked these links?
let urls=
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| distinct link;
let ips=
OutboundNetworkEvents
| where url in (urls)
| distinct src_ip;
Employees
| where ip_addr in (ips)
| distinct role
Answer: 8

How many Dark Magic Regulation Experts clicked the links?
let urls=
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| distinct link;
let ips=
OutboundNetworkEvents
| where url in (urls)
| distinct src_ip;
Employees
| where ip_addr in (ips)
| summarize count() by role
Answer: 4

What time did the first employee click on one of the links?
let urls=
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| distinct link;
OutboundNetworkEvents
| where url in (urls)
Answer: 2023-10-16T07:48:29.000Z

What is that employee's username?(The one who first clicked on the link)
let urls=
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| distinct link;
let first_ip=
OutboundNetworkEvents
| where url in (urls)
| where timestamp == datetime(2023-10-16T07:48:29.000Z)
| distinct src_ip;
Employees
| where ip_addr in (first_ip)
Answer: sugupta

What is the most common user agent used by this employee? (Format Firefox/XX.X)
let urls=
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| distinct link;
let first_ip=
OutboundNetworkEvents
| where url in (urls)
| where timestamp == datetime(2023-10-16T07:48:29.000Z)
| distinct src_ip;
let user=
Employees
| where ip_addr in (first_ip)
| distinct username;
AuthenticationEvents
| where username in (user)
| summarize count() by user_agent
Answer: Firefox/47.0
Notes: The other one was Firefox/11.0

When was there a login attempt from a different user agent for this employee? Copy and paste the exact timestamp
let urls=
Email
| where sender has_any ("executivecompliance@verizon.com", "executive_compliance@protonmail.com", "executive_compliance@yahoo.com", "compliance.compliance@verizon.com")
| distinct link;
let first_ip=
OutboundNetworkEvents
| where url in (urls)
| where timestamp == datetime(2023-10-16T07:48:29.000Z)
| distinct src_ip;
let user=
Employees
| where ip_addr in (first_ip)
| distinct username;
AuthenticationEvents
| where username in (user)
| where user_agent contains "Firefox/11.0"
Answer: 2023-10-17T01:17:35.000Z

From what IP address did this authentication occur?
Answer: 173.188.151.49

How many login attempts were seen from this IP?
AuthenticationEvents
| where src_ip == "173.188.151.49"
Answer: 3

Which user account was successfully logged in to from this IP? Enter the username
Answer: almcwaters

Let's look back at all AuthenticationEvents. Which user agent was used the most? (Format Firefox/XX.X)
AuthenticationEvents
| summarize count() by user_agent
Answer: Firefox/69.0

How many user accounts had login attempts from this user agent?
AuthenticationEvents
| where user_agent contains "Firefox/69.0"
| distinct username
Answer: 405

How many IP addresses were used with this user agent?
AuthenticationEvents
| where user_agent contains "Firefox/69.0"
| distinct src_ip
Answer: 2635

How many user account were successfully logged into using this user agent?
AuthenticationEvents
| where user_agent contains "Firefox/69.0"
| where result contains "successful"
| distinct username
Answer: 28

This type of attack is known as a __ .
Answer: password spray

What is the email subject that the threat actor used?
let comp_users=
AuthenticationEvents
| where user_agent contains "Firefox/69.0"
| where result contains "successful"
| distinct username;
let comp_emails=
Employees
| where username in (comp_users)
| distinct email_addr;
Email
| where sender in (comp_emails)
| where link has_any ("wizard-safety.com","wizardregulations.com","safetycontrol.com","control-regulations.com","wizardsafety.com","safetyregulations.com")
| distinct subject
Answer: URGENT: Please provide locations where your research is located.

Checking the internal network events, what document did they look within the enclave's secretive shares?
InboundNetworkEvents
| where src_ip == "173.188.151.49"
Answer: important_research.docx

What website did they use to get a refresher? Copy and paste the full URL.
OutboundNetworkEvents
| where url has "recon"
Answer: https://svch0st.medium.com/active-directory-recon-cheat-sheet-76ccc16dc6e8

Continue to investigate the threat actor. They may have downloaded and used an archiving tool. What url did they use to download it?
let recpt_email=
Email
| where subject == "URGENT: Please provide locations where your research is located."
| distinct recipient;
let phished_ip=
Employees
| where email_addr in (recpt_email)
| distinct ip_addr;
OutboundNetworkEvents
| where src_ip in (phished_ip)
| where url contains "exe"
Answer: https://www.7-zip.org/a/7z2002-x64.exe

What is the timestamp of the last file they archived and created?
let recpt_email=
Email
| where subject == "URGENT: Please provide locations where your research is located."
| distinct recipient;
let phished_host=
Employees
| where email_addr in (recpt_email)
| distinct hostname;
let zipped_files=
FileCreationEvents
| where hostname in (phished_host)
| where filename contains ".7z"
| distinct filename;
FileCreationEvents
| where filename in (zipped_files)
Answer: 2023-11-03T10:24:43Z


Section 2: Forest Blizzard
Let's take a look at our security alerts. It looks like there was a file that was quarantined on October 2, 2023. How many files with this filename is present on Scholomance hosts?
SecurityAlerts
| where timestamp between (datetime(10/2/2023, 12:00:00AM) .. datetime(10/3/2023, 12:00:00AM))
| where description contains "quarantined" //details.zip
Then:
FileCreationEvents
| where filename == "details.zip"
Answer: 15

How many roles were impacted with this file?
let comp_hosts=
FileCreationEvents
| where filename == "details.zip"
| distinct hostname;
Employees
| where hostname in (comp_hosts)
| summarize count() by role
Answer: 9

Let's pivot to emails. Identify all of the unique senders for this file. How many unique domains did they use to serve this file?
Email
| where link contains "details.zip"
| distinct link
Answer: 5

Look for all the files this threat actor served. How many total emails were sent by this threat actor containing potentially malicious files?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
Email
| where sender in (mal_sender)
Answer: 69

How many of these files were present on hosts?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let mal_file=
Email
| where sender in (mal_sender)
| distinct link
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| project file;
FileCreationEvents
| where filename in (mal_file)
Answer: 66

How many total job roles were targeted?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let mal_file=
Email
| where sender in (mal_sender)
| distinct link
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| project file;
let comp_host=
FileCreationEvents
| where filename in (mal_file)
| distinct hostname;
Employees
| where hostname in (comp_host)
| distinct role
Answer: 12

Let's start investigating what happens after these emails were sent & downloaded onto these hosts. The threat actor is staging files somewhere. What is the full path to the directory?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let filess = Email
| where sender in (mal_sender)
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| distinct file;
let comp_hosts=
FileCreationEvents
| where filename in (filess)
| distinct hostname;
ProcessEvents
| where hostname in (comp_hosts)
| where process_commandline contains ".bat"
Answer: C:\ProgramData

It may be worth it to start conducting open source intelligence on these files. Some of these files are tied to the previous section. What other group, named by Mandiant, are these other files associated with?
Answer: APT28

Now that you have some information on this threat, what filename (minus the extension) is used for collecting credentials?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let comp_email = Email
| where sender in (mal_sender)
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| distinct recipient;
let comp_hosts=
Employees
| where email_addr in (comp_email)
| distinct hostname;
ProcessEvents
| where hostname in (comp_hosts)
| where process_commandline contains "curl"
Answer: dc529177-39b4-4828-8c66-79fe35145d07

It looks like they may have dumped something from a native Windows database and saved it somewhere. What's the name of this file?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let comp_email = Email
| where sender in (mal_sender)
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| distinct recipient;
let comp_hosts=
Employees
| where email_addr in (comp_email)
| distinct hostname;
ProcessEvents
| where hostname in (comp_hosts)
| where process_commandline contains "save"
Answer: software.save

What's being used by the threat actor as a bootloader?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let comp_email = Email
| where sender in (mal_sender)
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| distinct recipient;
let comp_hosts=
Employees
| where email_addr in (comp_email)
| distinct hostname;
ProcessEvents
| where hostname in (comp_hosts)
| where process_commandline contains "start"
Answer: msedge

What GUID is being used in conjunction with this?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let comp_email = Email
| where sender in (mal_sender)
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| distinct recipient;
let comp_hosts=
Employees
| where email_addr in (comp_email)
| distinct hostname;
ProcessEvents
| where hostname in (comp_hosts)
| where process_commandline contains "msedge" and process_commandline contains "whoami"
Answer: aaccd6de-ce95-4fb2-b2c1-2d7ca08661a3

What filename were they using to stage the data?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let comp_email = Email
| where sender in (mal_sender)
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| distinct recipient;
let comp_hosts=
Employees
| where email_addr in (comp_email)
| distinct hostname;
ProcessEvents
| where hostname in (comp_hosts)
| where process_commandline contains ".zip"
Answer: protect.zip

What's the destination where the data was likely being transferred to?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let comp_email = Email
| where sender in (mal_sender)
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| distinct recipient;
let comp_hosts=
Employees
| where email_addr in (comp_email)
| distinct hostname;
ProcessEvents
| where hostname in (comp_hosts)
| where process_commandline contains "dc529177-39b4-4828-8c66-79fe35145d07"
Answer: https://webhook.site/dc529177-39b4-4828-8c66-79fe35145d07

What did the threat actor do for defense evasion? What did they disable?
let mal_sender=
Email
| where link contains "details.zip"
| distinct sender;
let comp_email = Email
| where sender in (mal_sender)
| extend filepath = tostring(parse_url(link).Path)
| extend file = tostring(parse_path(filepath).Filename)
| distinct recipient;
let comp_hosts=
Employees
| where email_addr in (comp_email)
| distinct hostname;
ProcessEvents
| where hostname in (comp_hosts)
| where process_commandline contains "disable" or process_commandline contains "delete"
| distinct process_commandline
Answer: LSA

Both of these threat actor groups are part of which specific agency from their country of origin?
Answer: GRU
